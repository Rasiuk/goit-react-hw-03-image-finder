// export const ImageGalleryItem = ({ hits: { id, webformatURL } }) => {
//   hits.map(hit => {
//     return (
//       <li key={id} className="gallery-item">
//         <img src={webformatURL} alt="" />
//       </li>
//     );
//   });
// };
